# 예외 인벤토리 대조표 (E1–E8)

기존 예외 목록(`parser/PARSING_NOTES.md`, `parser/README.md`)과 기존 변환기
코드(`parser/*.py`)를 명세의 8개 항목과 대조한 결과다.

분류 기준

| 분류 | 뜻 |
|---|---|
| **기존에 있음** | 기존 코드가 이 상황을 실제로 처리한다. 근거 코드 위치를 적었다. |
| **기존에 있음(암묵적)** | 처리는 되지만 "이 예외를 처리했다"는 의도가 코드에 없다. 다른 규칙의 부수효과다. 규칙이 바뀌면 조용히 깨진다. |
| **기존에 없음** | 처리하지 않는다. |
| **해당 없음** | 그 문서군/경로에 이 상황이 구조적으로 존재하지 않는다. |

가장 중요한 전제 — **기존 변환기는 lxml을 쓰지 않는다.** `html.parser`를 상속한
자체 관대 파서(`_TreeBuilder`)로 트리를 세운다. 그래서 절대 규칙 3(`recover=True`
금지)·4(bytes 금지)는 "기존 코드가 위반하고 있는 것"이 아니라 **XML 파서로
갈아탈 경우 밟게 될 지뢰**다. E1·E2가 "기존에 있음"인 것도 이 설계 덕분이다.

---

## E1 — bare `&`

**분류: 기존에 있음 (처리 O / 기록 X)**

| 항목 | 내용 |
|---|---|
| 근거 코드 | [major_parser.py:149-155](../parser/major_parser.py#L149-L155) `handle_entityref` (holding:‑, periodic:‑, exchange:213 동일 로직) |
| 기존 목록 | 공통 예외 3 "`&이름` 뒤에 세미콜론을 함부로 붙이면 안 됨" |
| 처리 방식 | `html5` 엔티티 사전에 있는 이름일 때만 실제 문자로 바꾸고, 아니면 `&이름`을 **원문 그대로** 되돌린다. |

기존 목록이 기록한 실측: 무조건 `&이름;`으로 복원했더니 원문 `삼성E&A` →
`삼성E&A;`로 깨진 사례가 exchange에서 **73건**. major 598건 중 `&` 이스케이프
누락이 **103파일**([major_parser.py:47-49](../parser/major_parser.py#L47-L49)).

**남는 구멍**: 몇 건을 어떻게 처리했는지 **아무 데도 기록하지 않는다.** 절대 규칙 2
("처리했더라도 처리했다는 사실이 산출물에 남아야 한다")를 만족하지 않는다.
4단계에서 `record: count`를 붙일 자리다.

---

## E2 — bare `<`

**분류: 기존에 있음 (처리 O / 기록 X)**

| 항목 | 내용 |
|---|---|
| 근거 코드 | `html.parser`의 기본 동작 + [major_parser.py:45-48](../parser/major_parser.py#L45-L48) 주석에 명시된 설계 근거 |
| 기존 목록 | 공통 예외 2 "DSD-XML은 정식 XML이 아님" |
| 처리 방식 | `<` 다음이 영문자일 때만 태그로 인식. 한글·숫자·기호가 오면 글자로 남긴다. |

기존 목록의 실측: major 598개 중 **189개(32%)가 XML 규격 위반**, 그중 `<별표3-3>`
같은 꺾쇠가 **87파일**. 명세가 말한 `< TV 시장점유율 추이 >`(표 캡션)와 같은 계열
이지만, DSD-XML에서는 캡션 말고 본문 꺾쇠가 주다.

**남는 구멍**: E1과 같다 — 처리 건수가 산출물에 남지 않는다.

---

## E3 — HTML charset 오선언

**분류: 기존에 있음**

| 항목 | 내용 |
|---|---|
| 근거 코드 | [exchange_parser.py:61-75](../parser/exchange_parser.py#L61-L75) `decode()` |
| 기존 목록 | 공통 예외 1 "인코딩 표시를 믿을 수 없음" |
| 처리 방식 | `<meta charset>`을 **읽지 않는다.** 무조건 UTF-8로 디코드하고, 실패할 때만 cp949로 폴백(`errors='replace'`). |

기존 목록의 실측: exchange HTML **1,469/1,469가 `charset=euc-kr`로 선언하지만
실제 바이트는 전부 UTF-8**. 절대 규칙 4가 말하는 "bytes를 넘기면 한글 전량 파괴,
예외 없음"을 기존 코드는 `decode()`를 강제해 이미 막고 있다
([holding_parser.py:976](../parser/holding_parser.py#L976) 등 모든 진입점이
`decode(open(path,'rb').read())` 형태).

**남는 구멍**: cp949 폴백이 발동했는지가 기록되지 않는다. `errors='replace'`는
조용히 글자를 `�`로 바꾼다 — 삼키는 예외다.

---

## E4 — `LIBRARY` 컨테이너

**분류: 기존에 있음(암묵적) — 검증 장치는 없음**

| 항목 | 내용 |
|---|---|
| 근거 코드 | major: [major_parser.py:297](../parser/major_parser.py#L297) `_PASSTHROUGH = {'BODY','LIBRARY','TABLE-GROUP',...}` + [major_parser.py:400](../parser/major_parser.py#L400) 마지막 catch-all.<br>holding·periodic: **catch-all 재귀 한 줄이 전부.** `_PASSTHROUGH` 자체가 없다. |
| 기존 목록 | **없음.** 예상대로 md 파이프라인이 예외를 던지지 않아 목록에 오르지 못했다. |

`grep -c LIBRARY parser/*.py` → major **1**, holding **0**, periodic **0**.
그런데 실측상 holding에 2,025개, periodic에 26,309개의 `LIBRARY` 노드가 있고
전부 정상 통과한다. 즉 **`LIBRARY`라는 문자열을 한 번도 본 적 없는 코드가
28,334개의 LIBRARY 컨테이너를 올바르게 훑고 있다.** major만 이름을 알고 있고,
그마저도 "제목 없이 통과시킬 태그" 목록에 우연히 들어가 있는 것이다.

기존 `_walk`는 태그를 알든 모르든 마지막에 무조건 자식으로 재귀한다. 그래서
`LIBRARY`가 중간에 끼어도 `SECTION-2`에 **도달은 한다.** 명세가 말한 "30% 유실"은
`//SECTION-1/SECTION-2` 같은 **직속 자식 경로**를 쓸 때 생기는 문제이고, 기존
코드는 그런 경로를 쓰지 않는다(절대 규칙 5를 이미 지키고 있다).

다만 두 가지가 걸린다.

1. **의도가 코드에 없다.** major에서 `LIBRARY`가 `_PASSTHROUGH`에 있는 건
   "제목 없이 통과"시키기 위해서지 "컨테이너 유실 방지" 때문이 아니다.
   holding·periodic에는 그 이름조차 없다. 누가 catch-all 재귀를
   "알 수 없는 태그는 건너뛴다"로 바꾸면 조용히 30%가 사라지고,
   **바꾼 사람은 자기가 무엇을 껐는지 알 방법이 없다** — 지운 줄에는
   LIBRARY라는 말이 안 적혀 있기 때문이다.
2. **`//SECTION-2` 개수 = 도달 개수를 확인하는 장치가 없다.** 검증 골든셋 4번
   (`structure`)이 바로 이걸 세우기 위한 것이다.

같은 계열의 실제 사고가 기존 목록에 하나 있다 —
[holding_parser.py:379-390](../parser/holding_parser.py#L379-L390):
`TITLE`이 `SECTION-N` 바로 밑에만 있다고 가정해서 `TABLE-GROUP` 밑 제목을
**136개 중 83개(61%)** 놓쳤던 버그. 원인 구조가 E4와 똑같다(컨테이너가 한 겹 껴서
직속 자식 가정이 깨진다).

---

## E5 — 표 아닌 TABLE

**분류: 기존에 있음 (md 렌더링 한정) / 구조화 경로에는 없음**

| 항목 | 내용 |
|---|---|
| 근거 코드 | [major_parser.py:464-474](../parser/major_parser.py#L464-L474) `_one_cell()`, 표 4분류 ③ |
| 기존 목록 | major 헤더 주석의 "표 4분류 ③ 1행 1칸 1,538개(27%) → 제목 962 / 번호제목 359 / 각주 124 …" |
| 처리 방식 | 1행 1칸 표는 표로 렌더링하지 않고 제목(`h`) 또는 문단(`p`)으로 강등한다. |

즉 **md에서는** 이미 "이건 표가 아니다"를 판정한다. 그러나 판정 기준이
`rows<=1 or cols<=1`이 아니라 "셀이 정확히 1개"이고, 판정 결과가
`parse_confidence` 같은 형태로 **밖으로 나오지 않는다.** 2단계 `table_router.py`가
받을 자리.

---

## E6 — 단위/각주 귀속 방향

**분류: 기존에 없음**

| 항목 | 내용 |
|---|---|
| 근거 코드 | `parser/*.py` 전체에서 `단위`를 의미 있게 다루는 코드 **0곳** (`grep -n 단위 parser/*.py` → 주석 4곳뿐) |
| 기존 목록 | **없음.** 예상대로. |

`(단위 : 억원, %)` 1×1 표는 [major_parser.py:474](../parser/major_parser.py#L474)
의 `return [('p', text)]`로 흘러가 **떠 있는 문단 하나**가 된다. 어느 표의
단위인지는 어디에도 남지 않는다. md를 사람이 읽을 때는 바로 위/아래라 붙지만,
청크로 쪼개는 순간 단위와 숫자가 서로 다른 청크로 갈라진다.

각주(`※`)도 마찬가지다 — [major_parser.py:291](../parser/major_parser.py#L291)에
`_RE_FOOTNOTE`가 정의만 되어 있고 `_one_cell()`은 이걸 쓰지 않는다(제목 정규식
두 개만 검사하고 나머지는 전부 문단). **정의해 놓고 안 쓰는 정규식**이다.

전수 실측 결과 명세의 방향은 맞았다 — 단위 캡션 318,391건 중 **뒤에만** 붙은 것이
54.8%인데 **앞에만** 붙은 것은 0.2%뿐이다. 다만 각주는 조건이 하나 더 붙는다:
각주 캡션 12,055건 중 **45.2%는 양옆 어디에도 진짜 표가 없다.** "각주표는 이전
형제에 귀속"을 무조건 적용하면 없는 귀속을 만들어낸다. 아래 실측표 참조.

---

## E7 — 회계기간 부재

**분류: 기존에 없음**

| 항목 | 내용 |
|---|---|
| 근거 코드 | 기수(`제 N 기`)를 날짜에 매핑하는 코드 **0곳** |
| 기존 목록 | **없음.** |

기존 코드에 날짜를 다루는 곳은 [major_parser.py:736](../parser/major_parser.py#L736)
`AUNITVALUE`를 사람이 읽는 날짜로 바꾸는 함수뿐이다. 이건 **셀 하나의 값**을
포맷하는 것이지, `제 55 기 1분기말`이라는 **헤더 라벨**에 실제 기준일을 물려주는
일이 아니다. 헤더는 `('t', headers, rows)`의 문자열로 그대로 나가고, 그 기수가
어느 날짜인지는 별도 캡션 TABLE에 남아 연결이 끊긴다.

---

## E8 — ACODE 반복

**분류: 해당 없음(md 경로) / 기존에 없음(구조화 경로)**

| 항목 | 내용 |
|---|---|
| 근거 코드 | [major_parser.py:818-852](../parser/major_parser.py#L818-L852) `to_dict()` |
| 기존 목록 | **없음.** |

명세가 경고한 `{acode: value}` dict 저장은 기존 코드에 **없다.** `to_dict()`는
라벨 경로(`' > '.join(parts)`)를 키로 쓰고, 같은 키가 또 나오면 `#2`, `#3`을 붙여
피한다([major_parser.py:824-828](../parser/major_parser.py#L824-L828)). 그래서
명세가 말한 "1,741→130, 93% 유실"은 이 코드에서는 일어나지 않는다.

대신 **다른 손실**이 있다.

- `to_dict()`는 `--json` 산출물 전용이고 **md 경로는 이 함수를 지나지 않는다.**
  md에서 ACODE는 어디에도 안 남는다(코드는 `('kv', parts, value, code, norm)`
  튜플에 실려 있다가 렌더링에서 버려진다).
- `#n` 접미사는 **문서 안 등장 순서**에 의존한다. 표가 하나 늘거나 순서가 바뀌면
  `항목명#3`이 다른 값을 가리킨다. 위치 기반 키가 아니다.

즉 E8은 "명세가 경고한 그 실수는 안 했지만, 쓸 만한 키도 아직 없다"가 정확한
상태다. 5단계 `extract/acode.py`가 만들 것.

**그리고 명세가 지정한 키도 못 쓴다.** 전수 실측 결과
`(table_idx, row_idx, acode)`는 7,635,332개 중 **27.45%를 잃는다.** 같은 행
안에서 같은 ACODE가 여러 열에 반복되기 때문이고(당기/전기, 지배/비지배 같은
다열 표), 실측 2,095,725건이다. 열 번호를 넣은
`(table_idx, row_idx, col_idx, acode)`는 유실이 **0.00%**다.
→ 5단계는 명세의 3튜플이 아니라 **4튜플**로 간다. "dict 금지"라는 명세의 의도는
그대로 지키되 키를 한 칸 넓힌다. 아래 실측표 참조.

---

## 요약

| # | 문제 | 분류 | 기존 목록 | 남는 일 |
|---|---|---|---|---|
| E1 | bare `&` | 기존에 있음 | 공통 3 | 처리 건수 기록 |
| E2 | bare `<` | 기존에 있음 | 공통 2 | 처리 건수 기록 |
| E3 | charset 오선언 | 기존에 있음 | 공통 1 | cp949 폴백·`replace` 발동 기록 |
| E4 | `LIBRARY` | 기존에 있음(암묵적) | **없음** | `structure` 검증으로 못 박기 |
| E5 | 표 아닌 TABLE | 기존에 있음(md 한정) | major 표 4분류 | `table_router` + `parse_confidence` |
| E6 | 단위/각주 귀속 | **기존에 없음** | **없음** | 신규. 단위=다음 형제 확정, 각주는 옆에 진짜 표가 있을 때만 |
| E7 | 회계기간 부재 | **기존에 없음** | **없음** | 신규 |
| E8 | ACODE 반복 | 해당 없음/없음 | **없음** | 신규. **명세의 3튜플 키는 27.45% 유실 — 4튜플로 간다** |

명세의 예상("E4·E6·E8은 기존 목록에 없을 가능성이 높다")은 맞았다. E7도 없었다.
E4는 **코드에는 있는데 목록에 없는** 경우로, 우연히 지켜지고 있어 회귀 위험이 가장
크다.

실측이 명세를 뒤집은 것이 하나 있다 — **E8의 키**. 명세가 지정한
`(table_idx, row_idx, acode)`는 이 코퍼스에서 27.45%를 잃는다. 열 번호를 더해야
0%가 된다. 명세의 탐지법을 그대로 구현해 재 보지 않았으면 5단계에서 조용히
1/4을 버렸을 것이다.

---

## 실측 (`scripts/01_exception_census.py`)

<!-- CENSUS:BEGIN -->
원문 **4,616개**를 읽어 다시 잰 값이다 (명세의 숫자를 옮겨 적지 않았다).

### 문서군별

| 문서군 | 문서 | E1 bare `&` | E2 bare `<` | E3 한글<5% | E4 `//SECTION-2` 정규식 → 도달 | LIBRARY 노드 | E5 TABLE | E5 표 아님 | E6 단위캡션 | E7 기수표기 | E8 TE(ACODE) |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| exchange | 1,469 | 92건 / 89문서 | 0건 / 0문서 | 0 | — | — | — | — | — | 9 | — |
| major | 598 | 130건 / 103문서 | 271건 / 87문서 | 0 | 14 → 14 (유실 0) | 1,005 | 5,609 | 2,559 (46%) | 586 | 124 | 56,740 |
| holding | 1,083 | 1,718건 / 206문서 | 250건 / 70문서 | 0 | 10,719 → 10,719 (유실 0) | 2,025 | 33,011 | 9,181 (28%) | 2,859 | 27 | 1,309,286 |
| periodic | 1,466 | 82,227건 / 1,311문서 | 41,386건 / 1,195문서 | 0 | 42,023 → 42,023 (유실 0) | 26,309 | 1,538,775 | 834,439 (54%) | 314,946 | 154,888 | 6,269,306 |
| **합계** | 4,616 | 84,167건 / 1,709문서 | 41,907건 / 1,352문서 | 0 | 52,756 → 52,756 (유실 0) | 29,339 | 1,577,395 | 846,179 (54%) | 318,391 | 155,048 | 7,635,332 |

exchange는 HTML이라 SECTION·TE·ACODE 자체가 없다. E4~E8은 0건이 아니라 **측정 대상 아님**이라 `—`로 둔다 — 0건과 "잴 것이 없음"은 다른 사실이다. E1~E3·E7은 원문 텍스트 기준이라 exchange에도 그대로 적용된다.

### 항목별 결론

**E3** — 5% 미만 문서 **0건**. 최저 `holding_20250110000695` 12.0%, `holding_20260311004186` 12.3%, `holding_20260226006888` 12.3%.

> 탐지법을 한 번 고쳤다. 태그를 정규식으로 지운 문자열을 분모로 쓰면 DSD-XML의 긴 속성이 분모를 부풀려, 한글 음절이 198,955개나 되는 멀쩡한 13.7MB 문서(`periodic_20251114002900`)가 2.84%%로 나와 "한글 전량 파괴"로 잡혔다. E3의 존재 이유가 그 파괴를 잡는 것인데 멀쩡한 문서에서 울리면 검사가 아니라 소음이다. → **파서가 실제로 읽어낸 본문**을 분모로 쓴다.

**E4** — `//SECTION-2` 정규식 52,756개 / 순회 도달 52,756개 / **유실 0개**. LIBRARY 컨테이너는 29,339개로 실재한다.

기존 `_walk`의 catch-all 재귀가 컨테이너를 그냥 통과하기 때문에 유실이 0이다. 명세가 경고한 30%% 유실은 **직속 자식 경로를 쓸 때** 생기며, 기존 코드는 그 경로를 쓰지 않는다. 다만 이건 의도가 아니라 부수효과라서, `structure` 검증으로 못 박아 두지 않으면 언제든 조용히 깨진다.

**E5** — TABLE 1,577,395개 중 **846,179개(53.6%)가 `rows<=1 or cols<=1`**.

**E6** — 귀속 방향을 "바로 옆 형제가 진짜 표인가"로 쟀다.

| 캡션 | 건수 | 뒤에만 | 앞에만 | 양쪽 | 없음 |
|---|---:|---:|---:|---:|---:|
| `(단위 : …)` | 318,391 | **54.8%** | 0.2% | 43.9% | 1.1% |
| `※ …` | 12,055 | 10.0% | **31.8%** | 13.0% | 45.2% |

단위표는 **다음 형제에 귀속**이 확정적이다 — 뒤쪽에만 붙은 것이 54.8%인데 앞쪽에만 붙은 것은 0.2%뿐이다. 명세대로다.

각주는 명세대로 **이전 형제** 쪽으로 기울지만(앞에만 31.8% vs 뒤에만 10.0%), **45.2%는 양옆 어디에도 진짜 표가 없다.** 즉 각주 캡션의 상당수는 표의 각주가 아니라 그냥 떠 있는 주석이다. "각주표는 이전 형제에 귀속"을 무조건 적용하면 없는 귀속을 만들어낸다 — 옆에 진짜 표가 있을 때만 붙여야 한다.

**E7** — 기수 표기(`제 N 기`) 155,048건. 이걸 실제 날짜에 매핑하는 코드는 기존에 0곳이다.

**E8** — 키 후보별 유실률. ACODE를 가진 TE **7,635,332개** 기준.

| 키 | 살아남는 레코드 | 유실률 |
|---|---:|---:|
| `{acode: value}` (명세가 경고한 것) | 1,006,191 | **86.8%** |
| `(table_idx, row_idx, acode)` (명세가 **요구한** 것) | 5,539,607 | **27.45%** |
| `(table_idx, row_idx, col_idx, acode)` | 7,635,332 | **0.00%** |

**명세가 지정한 키도 안전하지 않다.** `(table_idx, row_idx, acode)`는 27.45%를 잃는다. 원인은 **같은 행 안에서 같은 ACODE가 여러 열에 반복**되는 구조(당기/전기, 지배/비지배 같은 다열 표)이고, 실측 2,095,725건이다. 열 번호를 키에 넣으면 유실이 0.00%로 떨어진다.

→ 5단계 `extract/acode.py`의 키는 명세의 3튜플이 아니라 **`(table_idx, row_idx, col_idx, acode)`** 로 간다. 명세의 "dict 금지"라는 의도는 그대로 지키되, 키 자체를 한 칸 넓힌다.

### E2 bare `<` 실제 표본

| 문서 수 | 원문 조각 |
|---:|---|
| 171 | `<이사ㆍ감사 전체의 보수현황>` |
| 120 | `<당기말>` |
| 114 | `<보수지급금액 5억원 이상인 이사ㆍ감사의 개인별 보수현황>` |
| 103 | `<전기말>` |
| 72 | `<별표3-2>` |
| 71 | `<별표3>` |
| 71 | `<별표3-3>` |
| 71 | `<보수지급금액 5억원 이상 중 상위 5명의 개인별 보수현황>` |
| 68 | `<삭제>` |
| 62 | `<손해보험업 부문_메리츠화재>` |
| 55 | `<당분기말>` |
| 44 | `<통화선도>` |

<sub>생성: `scripts/01_exception_census.py` → `scripts/01b_update_matrix.py`. 원자료 `reports/exception_census.jsonl`.</sub>
<!-- CENSUS:END -->
