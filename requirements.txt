# 파싱·정규화 (scripts/00~07, src/extract·normalize·chunk·render)
lxml>=5.0
PyYAML>=6.0
orjson>=3.9

# 임베딩·검색 (src/index, src/eval)
numpy>=1.26,<2
scipy>=1.13
pyarrow>=15.0
FlagEmbedding>=1.3          # bge-m3. transformers/sentence-transformers를 끌고 온다
# torch는 CUDA 버전에 맞춰 따로 설치한다. 이 레포를 돌린 환경은 2.7.1+cu118:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118

# PDF 정기보고서 경로(parser/pdf_periodic_parser.py)는 외부 pdf_parser 모듈을
# 참조한다. 레포에 포함되지 않으며, 없으면 해당 경로만 건너뛴다.
