# CLAUDE.md — 공시 Agent 프로젝트 상시 규칙

이 파일은 리포 루트에 두고 모든 세션에서 적용된다. 개별 작업 프롬프트보다 우선한다.

## 프로젝트

제10회 미래에셋증권 AI Festival · 공시 Agent.
DART 공시 코퍼스(70개사 / 4,204문서 / 2023.01~2026.03)로 질의응답 Agent를 만든다.
**예선 마감 2026-09-06, 실질 마감 09-04.** 마감 이후 커밋은 실격이다.

전처리·청킹·bge-m3 임베딩은 완료됨. 현재 검색기부터 만드는 중.

## 절대 규칙

### 1. 데이터를 지어내지 않는다
입력 파일에 있어야 할 필드가 없으면 **작업을 멈추고 보고한다.**
기본값으로 채우거나, 추론해서 만들거나, 빈 값으로 넘어가지 않는다.
이건 조용한 오염을 만들고 며칠 뒤 원인 불명으로 발견된다.

### 2. 스펙과 다르게 하고 싶으면 먼저 묻는다
`docs/plan/step*.md` 가 사양서다. 더 나은 방법이 보여도 **구현 전에 이유를 말하고 승인을 받는다.**
특히 아래는 이유가 있어 그렇게 정해진 것이니 임의 변경 금지:

- BM25를 scipy CSC로 직접 구현 (`bm25s`, `rank_bm25`, `elasticsearch` 사용 금지)
  — 하드필터 우선 구조에서 라이브러리의 전역 top-k 전제가 깨진다
- RRF는 각 리스트 top-M 안에 든 문서만 기여 (전체 순위 융합 금지)
- 정정공시는 기본 제외 (`is_latest`)
- `corp_name`은 검색 query 문자열이 아니라 별도 필터 파라미터

### 3. 행 순서 계약
`chunks.jsonl.gz` 의 행 순서 = `dense.f32.npy` 행 = `id_map.parquet` 행 = BM25 행.
**어떤 단계에서도 정렬·재배치하지 않는다.** 순서가 필요하면 인덱스 배열만 다룬다.
행 수가 하나라도 다르면 즉시 중단하고 보고한다.

### 4. 검증을 먼저 쓴다
구현 → 검증이 아니라 **검증 스크립트 작성 → 실행 → 구현 → 재실행** 순서.
검증 없이 "동작할 것"이라고 보고하지 않는다.

### 5. 실제 데이터로만 검증
mock·샘플·더미 데이터로 통과했다고 보고하지 않는다.
먼저 1,000건 스모크 → 수치 보고 → 승인 후 전량 실행.

### 6. 산출물 경로
쓰기 허용: `src/`, `eval/`, `config/`, `data/index/`, `reports/`, `docs/`
**읽기 전용(수정 금지): `data/raw/`, `data/processed/`, `data/index/vectors/`**
기존 파일을 덮어쓰기 전에 반드시 확인을 받는다.

### 7. 커밋
논리 단위마다 커밋한다. 한 커밋에 여러 단계를 섞지 않는다.
커밋 메시지는 `step1: BM25 색인 생성기` 처럼 단계 번호로 시작.

### 8. 리팩터링 금지
요청받지 않은 파일을 정리·개선·포매팅하지 않는다.
기존 코드에 손대야 하면 이유를 먼저 말한다.

### 9. 예외를 삼키지 않는다
`except: pass` 금지. 처리하되 반드시 `reports/` 에 기록을 남긴다.
과거 이 프로젝트에서 `AT&T` → `AT` 가 조용히 지나간 전례가 있다.

### 10. 도구 함수는 예외를 던지지 않는다
`src/tools/` 의 함수는 어떤 입력에도 `{"status": ...}` JSON을 반환한다.
평가 중 예외가 나면 그 문항은 0점이다.

## 보고 형식

작업이 끝나면 아래 형식으로만 보고한다. 코드를 다시 붙여넣지 않는다.

```
## 한 일
- (파일 경로: 한 줄 설명)

## 수치
- (실행 시간, 처리 건수, 메모리, 테스트 통과/실패)

## 계약 위반·이상
- (없으면 "없음")

## 다음에 필요한 결정
- (사람이 판단해야 할 것만. 없으면 "없음")
```

## 환경

- GPU: RTX 4090 24GB × 4 / CPU: Xeon Gold 6326 / RAM: 384GB
- 메모리 절약 최적화 불필요. 단순하고 읽기 쉬운 쪽을 택할 것
- LLM은 HyperCLOVA X만 사용 (과제 제약). 임베딩·리랭커는 로컬 GPU
- 외부 데이터 사용 금지 (뉴스·리포트·위키·OpenDART 실시간 호출 전부 금지)